{% extends 'base.html.twig' %}

{% block body %}
{{ parent() }}
    <div class="container">
        <h1>New Movie</h1>
        <div class="form-div">
            {% form_theme form 'bootstrap_4_layout.html.twig' %}
            {{ form_start(form) }}
                {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}